require! {
    fs
    path
    './commentsTemplates': { comments }
}

const libComments = (type) ->
    type

const determinLanguage = (filename) ->
    filename

const addComments = (filename, context) ->
    filename

export determinLanguage
export addComments