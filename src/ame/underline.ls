require! {
    path
    '../func/config': { getConfig, writeConfig }
}

const underline = (name?, progress?) ->
    name

const puls = (name) ->
    name

const minus = (name) ->
    name

export underline
export puls
export minus