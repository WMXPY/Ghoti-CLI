require! {
    path
    '../func/config': { getConfig, writeConfig }
}

const underline = (ghoti) ->
    ghoti

const puls = (name) ->
    name

const minus = (name) ->
    name

export underline
export puls
export minus