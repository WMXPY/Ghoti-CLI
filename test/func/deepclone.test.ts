import { expect } from 'chai';

describe('test deepclone', (): void => {

    // let before;

    // beforeEach((): void => {
    //     before = {
    //         aBoolean: true,
    //         aNumber: 16,
    //         aString: 'hello',
    //         anArray: ['whooo', 45]
    //     }
    // });

    // it('deepclone will not change element of object', (): void => {
    //     let clonedBefore = before;
    //     expect(before).to.be.deep.equal(clonedBefore);
    // });
});
