/**
 * @author ${|author|}
 * @overview generated by ghoti-cli
 * @fileoverview Lambda function set ${|test|}
 * @fileoverview Feature description ${|feature|}
 */

import { expect } from 'code';
import { shallow, ShallowWrapper, configure } from 'enzyme';

import * as React from 'react';
import * as Adapter from 'enzyme-adapter-react-16';

import * as Component from '../src/component/import';
import * as Func from '../src/func/import';
import * as Lambda from '../src/lambda/import';
import * as Page from '../src/page/import';

configure({
    adapter: new Adapter(),
});

describe('${|test|}', (): void => {

    let ${|test|}: ShallowWrapper<HTMLElement>;

    beforeEach((): void => {
        ${|test|} = shallow(<div>${|test|}</div>);
    });

    it('${|test|}', (): void => {
        expect(${|test|}.type()).to.be.equal('div');
    });

});