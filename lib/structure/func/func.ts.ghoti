/**
 * @author ${|author|}
 * @overview generated by ghoti-cli
 * @fileoverview Function set ${|func|}
 */

import Config from '../config/config';

const ${|func|} = (): void => {
    // Hello, ${|func|}!
};

const fetch${|func|} = async (): void => {
    // Hello, ${|func|}!
};

export {
    ${|func|},
    fetch${|func|},
};
